
export const initialState = {
  users:[],
  projects:[]
};

const searchReducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default searchReducer;
